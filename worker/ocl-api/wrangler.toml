name = "ocl-api"
main = "index.js"
compatibility_date = "2025-01-01"

# Account: 978dcaac35dcbe8c7c7c9b200c3db416 (Openchecklists@keylinkit.net) — same
# account as the openchecklists.net zone, so the openchecklists.net/api/* route works.
account_id = "978dcaac35dcbe8c7c7c9b200c3db416"

[[d1_databases]]
binding = "DB"
database_name = "openchecklists-users"
database_id = "d2ae495d-dfe6-4336-a2c8-7e80c8e950a5"

[vars]
ZITADEL_ISSUER = "https://auth.keylinkit.net"
OCL_ORIGIN = "https://openchecklists.net"

# Secrets (set via: wrangler secret put ANTHROPIC_API_KEY)
# ANTHROPIC_API_KEY — for AI checklist review
